package com.silverline.erp.infrastructure.sse;

/**
 * Defines the distinct real-time event channels for Server-Sent Events (SSE).
 */
public enum SseChannel {
    NOTIFICATIONS,
    PASSWORD_RESETS
}
